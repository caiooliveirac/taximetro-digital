import { PlantaoAoVivo } from "@/components/admin/plantao-ao-vivo";

/** O turno em andamento, base por base, para a coordenação intervir do celular. */
export default function AdminPlantaoPage() {
  return <PlantaoAoVivo />;
}
